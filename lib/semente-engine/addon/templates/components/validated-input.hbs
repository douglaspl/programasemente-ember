<div class="flex-col w100 mt1">
    <div>
        <label class="form-group__label">{{label}}</label>
    </div>
    <div>
        {{input class=(concatenate "w100 " classe) value=(get changeset propertyName)
        onblur=(action 'validateProperty' changeset property) }}
         
    </div>

    <div class="mt0">
        {{#if (get changeset.error propertyName)}}
            {{#each (get (get changeset.error propertyName) 'validation') as |error|}}
                <span class="color-error-40" style="font-size: 12px">{{error}}</span>
            {{/each}}
        {{/if}}
    </div>
</div>