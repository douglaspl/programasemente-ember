<div class="row">
    <h1>Origem</h1>
    <label for="">Endereço de Vídeo</label>
    {{input name='' value=conteudo.videoUrl placeholder='Endereço de Vídeo'}}
    
    <label for="">Arquivo PDF</label>
    {{input name='' value=conteudo.path placeholder='Arquivo PDF'}}
</div>

<div></div>
<div class="row">
    <h1>Configuração</h1>
    <label for="">Titulo do Conteúdo</label>
    {{input name='' value=conteudo.titulo placeholder='Titulo do Conteúdo'}}
    
    <div></div>
    <div>Faz parte de qual agrupamento?</div>

    {{#each agrupamento as |a|}}
        {{#radio-button value= a.name 
            groupValue= agrupamento.name 
            changed=(action "agrupamentoChanged") }}  
            <span>Agrupamento</span>
        {{/radio-button}}
    {{/each}}

    {{!-- {{#each agrupamento as |a|}}
        <li>{{a.name}}</li>
        {{!-- {{#if a.temas.size }} --}}
        {{!-- {{#each a.temas as |t|}}
            <li>{{t.name}}</li>
        {{/each}}
    {{/each}} --}}
    
    <div></div>
    <div>Qual a situação após o cadastro?</div>
    <div class="row">
        <label for="conteudoId">Gênero</label>
        <select name="genero" id="conteudoId" onchange={{action 'refreshSelectedSituacao' value='target.value'}}>
            <option value="">---</option>
            <option value="true"  selected={{if (equalstr 'selectedSituacao' "true") "selected"}}>Ativo</option>
            <option value="false"  selected={{if (equalstr 'selectedSituacao' "false") "selected"}}>Inativo</option>
        </select>
    </div>
</div>


<div></div>


<div class="row">
    <h1>Público</h1>
    <label for="">Quem se destina</label>
    {{#each publico as |pub|}}
        <li>{{pub.name}}</li>
    {{/each}}
</div> 


<button type="button" class="login__cta btn" {{action "saveConteudo" conteudo}}>Salvar</button>